Local patch applied on top of a8d2f4cf0c4c36d021debfe0428ec497660c55e6:

- Add HOST support in src/config.js.
- Bind src/server.js to config.host instead of 0.0.0.0.

Reason:

WindsurfProxyAPI keeps the inner pool private on 127.0.0.1:8328. The upstream
project defaults to 0.0.0.0, which would bypass the outer gateway boundary.
